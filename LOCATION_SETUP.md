# Running the location code

Use Python 3.11 or newer.

```bash
python -m venv .venv
```

Activate the environment, then install the two required packages:

```bash
python -m pip install -r requirements.txt
python locations.py
```

The district coordinates are in `district_locations.csv`.
